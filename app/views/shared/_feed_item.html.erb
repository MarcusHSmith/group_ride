<li id="<%= feed_item.id %>">
	<%= link_to gravatar_for(feed_item.user), feed_item.user %>
	<span class="content"><p2><%= link_to feed_item.title, feed_item %> </p2>    <%= feed_item.date.to_formatted_s(:short) %></span>
	<span class="content"><p><%= feed_item.content %></p></span>

</li>